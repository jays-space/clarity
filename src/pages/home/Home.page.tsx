import { Page } from "@/components/atomic";
import { HeroTemplate } from "@/components/templates";

const Home = () => {
  return (
    <Page>
      <HeroTemplate />
    </Page>
  );
};

export default Home;
